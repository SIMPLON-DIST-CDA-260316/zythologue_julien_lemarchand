{
  "name": "zythologue",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Raccourcis pnpm pour gérer l'environnement Docker PostgreSQL du brief Zythologue.",
  "imports": {
    "#features/*": "./src/features/*",
    "#config/*": "./src/config/*",
    "#http/*": "./src/http/*",
    "#errors/*": "./src/errors/*"
  },
  "scripts": {
    "dev": "nodemon --legacy-watch --watch src --watch server.js --exec \"node --env-file-if-exists=.env server.js\"",
    "start": "node --env-file-if-exists=.env server.js",
    "db:schema": "docker compose exec -T postgres sh -c \"psql -v ON_ERROR_STOP=1 -U $POSTGRES_USER -d $POSTGRES_DB -f /docker-entrypoint-initdb.d/01_create_schema.sql\"",
    "db:seed": "docker compose exec -T postgres sh -c \"psql -v ON_ERROR_STOP=1 -U $POSTGRES_USER -d $POSTGRES_DB -f /docker-entrypoint-initdb.d/02_seed.sql\"",
    "db:reset": "pnpm db:schema && pnpm db:seed",
    "db:init": "docker compose up -d --wait && pnpm db:reset",
    "db:psql": "docker compose exec postgres sh -c \"psql -U $POSTGRES_USER -d $POSTGRES_DB\"",
    "gen:mcd": "pwsh -NoProfile -File scripts/generate-diagrams.ps1",
    "gen:mcd:bash": "bash scripts/generate-diagrams.sh"
  },
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.11.0",
    "pg": "^8.22.0",
    "swagger-jsdoc": "^6.3.0",
    "swagger-ui-express": "^5.0.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
